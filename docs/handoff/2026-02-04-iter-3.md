# Handoff: 2026-02-04 (Iteration 3)

## Summary

Converted Codemm-IDE into a monorepo and removed the dependency on sibling folders (`../Codemm-backend`, `../Codemm-frontend`).

## What’s Done

- Imported backend + frontend into this repo (git subtree):
  - `apps/backend`
  - `apps/frontend`
- Moved Electron wrapper into:
  - `apps/ide/main.js`
  - `apps/ide/package.json`
- Added npm workspaces root:
  - `package.json` (repo root)
- Updated Electron boot logic to use in-repo apps:
  - runs `npm install` in repo root if needed
  - builds judge Docker images from `apps/backend/Dockerfile.*-judge` if missing
  - starts backend/frontend via npm workspaces
- Updated docs to match the new layout:
  - `README.md`, `CONTRIBUTING.md`, `docs/FUNCTIONS.md`, `docs/ARCHITECTURE.md`, `AGENTS.md`

## How To Run

From repo root:

```bash
npm install
npm run dev
```

## Notes / Known Issues

- This is still dev-mode:
  - backend runs via `npm run dev`
  - frontend runs via `next dev`
- Packaging into a distributable `.app` is the next major milestone.

## Next Steps

1. Regenerate/normalize lockfiles for npm workspaces (single root lockfile).
2. Electron hardening:
   - block unexpected navigations (allowlist local URLs)
   - tighten external-link handling
3. Packaging:
   - add `electron-builder`
   - build backend + frontend in prod mode and run from the `.app`
   - rebuild native deps (`better-sqlite3`) for Electron.

