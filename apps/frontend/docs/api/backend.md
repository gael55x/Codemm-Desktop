# Engine API (No HTTP)

The renderer does not call `fetch()` to `localhost`.

Authoritative references:

- Preload surface area: `apps/ide/preload.js`
- Engine methods: `apps/backend/src/ipcServer.ts`
